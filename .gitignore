# Track only the skill surfaces. Everything else in ~/.claude is machine state,
# session transcripts, or cache and must never be committed.
/*
!/.gitignore
!/README.md
!/skills/
!/skills-staging/
!/skills-disabled/
!/scripts/
!/hooks/
skills/**/.backups/
hooks/*.log
